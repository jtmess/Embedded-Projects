#include <stdio.h>

int main(int argc, char** argv)
{
  int spectranum=0, loopint=0;
  printf("Enter the number of spectra:\n");
  scanf("%d",spectranum);
  for (loopint=0;loopint<spectranum;loopint++)
  {
    char x[spectranum];
    /** prompt user for integration time */
    /** store integration time
    /** read data from spectrometer */
    i
    x[loopint]=data;
    y[loopint]=data;
  }
  mean functions
  graph mean functions
}
